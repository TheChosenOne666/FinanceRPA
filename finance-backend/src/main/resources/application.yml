server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: finance-backend
  # Java 21 虚拟线程启用（对齐 tech-stack ADR-005）
  threads:
    virtual:
      enabled: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
      - com.baomidou.mybatisplus.autoconfigure.MybatisPlusAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.servlet.SecurityFilterAutoConfiguration
      - org.springframework.boot.autoconfigure.security.reactive.ReactiveSecurityAutoConfiguration
      - org.springframework.boot.actuate.autoconfigure.security.servlet.ManagementWebSecurityAutoConfiguration

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true

info:
  app:
    name: finance-backend
    description: FinanceRPA Java 后端 - 企业管理与编排服务
    version: 0.0.1-SNAPSHOT

logging:
  level:
    root: INFO
    com.finrpa: DEBUG
    org.springframework: INFO

# MyBatis-Plus 配置（M0.2 启用数据源后生效）
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# springdoc-openapi 配置
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.finrpa

# JWT 配置
jwt:
  secret: finrpa-finance-rpa-jwt-secret-key-2026-longer-than-256-bits
  access-token-expire-minutes: 60
  refresh-token-expire-days: 7
